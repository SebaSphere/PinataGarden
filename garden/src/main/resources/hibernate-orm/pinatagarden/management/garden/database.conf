db = {
  connectionPool = "HikariCP" // use HikariCP for our connection pool
  dataSourceClass = null // Simple datasource with no connection pooling. The connection pool has already been specified with HikariCP.
  driverClassName = "org.h2.Driver" // h2 stuff
  jdbcUrl = "jdbc:h2:mem:testdb" // h2 stuff
  properties = {
    serverName = null // the IP needed to connect
    portNumber = null
    databaseName = null,
    user = "sa" // The username and password it attempts to connect with. Should be empty if h2 as this is local
    password = "sa"
  }
  numThreads = 10
}